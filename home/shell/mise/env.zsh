# Mise environment variables — kept minimal because mise self-bootstraps via `mise activate`.
# Add MISE_DATA_DIR / MISE_CONFIG_DIR overrides here if a host needs non-default locations.
export MISE_AUTO_INSTALL=false
